#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$script_dir/ai-agent-status-env"
ai_status_load_env

status_file="$AI_STATUS_CACHE_DIR/combined.txt"

if [[ -s "$status_file" ]]; then
  cat "$status_file"
else
  printf 'AI agents: idle\n'
fi
